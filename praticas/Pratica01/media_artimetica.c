#include <stdio.h>
#include <windows.h> 
int main()
{
SetConsoleOutputCP(65001);
int n;
int vet[n];
printf("Informe um número inteiro: \n");
scanf("%d", &n);
for (int i = 0; i < n; i++){

    printf("informe %d números: ", vet[n] - i);
    scanf("%d", vet[n]);
}

    return 0;
}